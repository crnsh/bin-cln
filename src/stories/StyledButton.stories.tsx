import StyledButton from "../components/StyledButton";

export default {
    component: StyledButton,
    title: 'StyledButton',
    tags: ['autodocs']
}

export const Default = {
    args: {
        children: "Hello"
    }
}